---
description: 森哥专属开发与回复规范
alwaysApply: true
---

# 森哥专属规则

## 代码结构

- 优先解耦：模块单一职责，避免高耦合与隐式依赖。
- 层次分明：按表现层/业务层/数据层组织，跨层调用必须通过清晰接口。
- 变更可控：新增功能优先扩展接口，不直接侵入不相关模块。

## 解耦细则（重点）

- 功能解耦：一个功能点只在一个核心模块实现，禁止多处重复业务逻辑。
- 文件解耦：每个文件只承载一类职责，超出职责范围必须拆分文件。
- 接口先行：模块之间通过稳定接口通信，禁止跨文件直接操作内部状态。
- 依赖单向：高层依赖抽象，底层实现可替换，避免循环依赖。
- 服务解耦：服务之间通过明确契约（API/事件）交互，不共享隐式上下文。
- 服务自治：每个服务独立部署、独立监控、独立故障隔离，避免连锁故障。
- 数据边界：服务拥有各自数据边界，跨服务访问必须走公开接口。

## 团队协作

- 统一分层约定与命名规范，评审时重点检查耦合风险与边界清晰度。
- 需求拆分优先按领域边界切任务，避免多人同时改同一核心文件。
- 合并前必须说明变更影响面、依赖关系与回滚方案。
- 新增公共能力先抽象接口并补充示例，减少团队重复实现。
- 关键公式或算法变更需附推导说明、单位定义与验证样例。
- 代码评审不只看功能可用，更要检查可维护性、可测试性与可扩展性。

## 解耦落地检查（提交前）

- 功能检查：本次新增逻辑是否只在一个领域入口实现，无重复拷贝。
- 文件检查：单文件是否只承担单一职责，混合职责需拆分。
- 依赖检查：是否引入循环依赖，是否出现跨层直接调用。
- 服务检查：跨服务调用是否通过契约，是否包含超时、重试和降级策略。
- 数据检查：是否直接读写他服务私有数据，若有必须改为接口访问。
- 测试检查：是否覆盖核心边界与失败分支，验证解耦后行为一致。

## 协作执行机制

- PR 描述必须包含：领域边界、接口变化、耦合风险、回滚方案。
- 评审意见必须包含：至少 1 条关于解耦或可维护性的检查结论。
- 合并策略：高风险改动采用小步合并，禁止一次性跨层大改。
- 任务分工：按模块边界分配 owner，避免多人同时侵入同一职责层。
- 复盘机制：线上问题复盘必须追踪到耦合根因并沉淀规则更新。

## 数学与公式

- 数学公式必须符号一致、单位明确、边界条件完整。
- 推导或计算前先定义变量含义，结果需可复核。
- 遇到近似或假设时，必须显式写明前提与适用范围。

## 回复风格

- 每次回复都称呼用户为“森哥”。
- 每次回复都附带一句简短、真诚的正向赞美。

## 示例

```text
❌ BAD: 在控制器直接拼接 SQL 并写业务规则。
✅ GOOD: 控制器 -> Service -> Repository，接口职责清晰。

❌ BAD: 一个“订单结算”逻辑散落在 5 个文件且多个服务重复实现。
✅ GOOD: 结算能力集中在领域服务，通过契约对外提供，其他模块仅调用接口。

❌ BAD: 直接写 E = m*a，不说明单位与条件。
✅ GOOD: 定义 m(kg)、a(m/s^2)，给出 F = m*a 并注明适用条件。
```
